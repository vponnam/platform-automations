az-configuration:
- name: us-east-2c
  iaas_configuration_guid: 420996cd47f403b40188
network-assignment:
  network:
    name: PCF-Private-Network
  other_availability_zones: []
  singleton_availability_zone:
    name: us-east-2c
networks-configuration:
  icmp_checks_enabled: false
  networks:
  - name: PCF-Private-Network
    subnets:
    - iaas_identifier: subnet-045b8ba8607a34e8e
      cidr: 10.0.10.128/25
      dns: 10.0.10.2
      gateway: 10.0.10.129
      reserved_ip_ranges: 10.0.10.128-10.0.10.135
      availability_zone_names:
      - us-east-2c
properties-configuration:
  director_configuration:
    allow_legacy_agents: true
    blobstore_type: local
    bosh_recreate_on_next_deploy: false
    bosh_recreate_persistent_disks_on_next_deploy: false
    database_type: internal
    director_worker_count: 5
    encryption:
      keys: []
      providers: []
    hm_emailer_options:
      enabled: false
    hm_pager_duty_options:
      enabled: false
    identification_tags: {}
    keep_unreachable_vms: false
    local_blobstore_options:
      tls_enabled: true
    ntp_servers_string: 0.north-america.pool.ntp.org, 1.north-america.pool.ntp.org
    post_deploy_enabled: false
    resurrector_enabled: false
    retry_bosh_deploys: false
    skip_director_drain: true
  dns_configuration:
    excluded_recursors: []
    handlers: []
  iaas_configuration:
    additional_cloud_properties: {}
    encrypted: false
    guid: ((properties-configuration_iaas_configuration_guid))
    iam_instance_profile: ((properties-configuration_iaas_configuration_iam_instance_profile))
    key_pair_name: ((properties-configuration_iaas_configuration_key_pair_name))
    kms_key_arn: ((properties-configuration_iaas_configuration_kms_key_arn))
    name: ((properties-configuration_iaas_configuration_name))
    region: ((properties-configuration_iaas_configuration_region))
    security_group: ((properties-configuration_iaas_configuration_security_group))
  security_configuration:
    generate_vm_passwords: true
    opsmanager_root_ca_trusted_certs: false
  syslog_configuration:
    enabled: false
resource-configuration:
  compilation:
    instances: automatic
    instance_type:
      id: automatic
    internet_connected: false
  director:
    instances: automatic
    persistent_disk:
      size_mb: automatic
    instance_type:
      id: m4.large
    internet_connected: false
vmextensions-configuration: []

